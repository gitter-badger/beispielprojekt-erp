<?php
if($this->success == "true") { ?>
<div class="alert alert-success alert-dismissible" role="alert">Bestellung ist eingetütet.
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
</div>
<?php } ?>

<form action="<?php echo $this->url('application', array('controller' => 'bestellung', 'action' => 'bestellen')); ?>" method="post">
    <div class="form-group">
        <label for="bezeichnung">Bezeichnung</label>
        <input type="text" class="form-control" name="bezeichnung" id="bezeichnung" placeholder="Bezeichnung">
        <p class="help-block">Hier wird die Bezeichnung für die Bestellung festgelegt.</p>
    </div>
    <div class="form-group">
        <label for="material">Material</label>
        <select name="material" id="material" class="form-control">
            <?php
            /* @var $material \Application\Entity\Material */
            foreach($this->materialien as $material) { ?>
            <option value="<?php echo $material->getId() ?>"><?php echo $material->getBezeichnung(). ' ('. $material->getPreis() .' EUR)' ?></option>
            <?php } ?>
        </select>
    </div>
    <div class="form-group">
        <label for="anzahl">Anzahl</label>
        <input type="text" class="form-control" name="anzahl" id="anzahl">
        <p class="help-block">Wieviel brauchen Sie denn davon?</p>
    </div>

    <input type="submit" class="btn btn-default" value="Bestellen" />
</form>
